/**
 * Combination represents a collection of items used to generate combinations.
 */
class Combination {
  /**
   * Creates an instance of the Combination class.
   *
   * @param {Array} items - An array of items to be combined.
   */
  constructor(items) {
    this.items = items
  }
}

export default Combination
